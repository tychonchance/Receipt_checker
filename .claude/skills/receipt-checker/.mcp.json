{
  "mcpServers": {
    "receipt-checker": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/mcp_server.py"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PROJECT_DIR}/backend"
      }
    }
  }
}
